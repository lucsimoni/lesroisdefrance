export interface Character {
    id: string,
    title: string,
    nickname: string,
    period: string,
    dateStart: number,
    dateEnd: number,
    imageUrl: string,
    prestige: number,
    predecessor: string,
    dateBorn: number,
    dateDeath: number
    dynasty: string,
    father: string,
    mother: string

    //Blason
    //Map debut
    //Map apogee
    //Map fin
    //Resume
    //galeries image array string
    //Résumé faits marquants array string
    //carousel img sur 6 droit array string
    //france debut reigne fin regne sur 6 droit
    //Histoire sur 6 gauche
    //Bouton retour en bas à gauche

    //page accueil
    //4 recherches + afficher tous les rois
}